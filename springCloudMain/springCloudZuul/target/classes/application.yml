server:
  port: 8100

spring:
  application:
    name: user-zuul

eureka:
  client:
    service-url:
      defaultZone: http://localhost:7003/eureka/
zuul:
  max:
    host:
      connections: 500
  host:
    socket-timeout-millis: 60000
    connect-timeout-millis: 60000
